from src.memory.pattern_learner import PatternLearner

# Example of adding custom debug patterns
pattern = {
    "type": "zero_division",
    "pattern": "division by zero",
    "solution": "Add check for zero denominator"
}